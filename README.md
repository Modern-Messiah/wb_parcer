# WB Parser

Простой парсер каталога Wildberries под тестовое задание.

## Установка

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Запуск

```bash
python main.py
```

По умолчанию запрос: `пальто из натуральной шерсти`.
